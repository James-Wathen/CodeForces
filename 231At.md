main for1 is looping through the same cin, each time lineArray seems to take 1 character from voteStr.
Should take the whole string at once and then wait for next input.
Despite that, the int array outputted seems very wrong.